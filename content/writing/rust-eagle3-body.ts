// Generated from the reviewed experiment WRITEUP.md. Do not edit independently.
const body = "<p>A small adaptation of an existing EAGLE-3 draft reduced single-request Rust generation latency by <strong>15.63% relative to the original draft</strong> in the matched-control study, with an exploratory 95% prompt-bootstrap interval of <strong>14.26–17.11%</strong>. The target, Qwen3-8B, stayed frozen. The result concerns serving speed on a small synthetic workload, not improved code quality.</p>\n<p>The more interesting finding was how we got there. The initial 512-example adaptation replicated at about 10.7% across two training seeds and two fresh serving sessions. Expanding the pool to 2,048 examples barely helped at 100 updates. Training for 400 updates helped much more. At that larger budget, the direct advantage of 2,048 over 512 examples was <strong>1.76%</strong> (0.40–2.84%). Rust specialization also slowed a small JSON-call diagnostic. A mixed-data draft improved prose relative to the Rust-only draft, at a small cost to Rust; it did not demonstrate a JSON repair.</p>\n<p><img loading=\"lazy\" alt=\"Matched-update control: latency and acceptance\" src=\"/artifacts/rust-eagle3/phase2/figures/control.png\" /></p>\n<p><em>Positive latency reduction means faster. Every comparison uses its own same-session baseline. Intervals describe prompt resampling conditional on these checkpoints and sessions.</em></p>\n<h2 id=\"the-question\">The question</h2>\n<p>An inference-engineering project should separate the value of an existing method from the value of a modification. EAGLE-3 already accelerates Qwen. This experiment asks whether adapting an existing draft to a narrow workload can improve it further, whether that gain repeats, and what happens as data and optimization budgets change.</p>\n<p>Rust is a useful bounded workload because generating functions requires many successive tokens, and speculative decoding can verify several proposed tokens together. We used mostly complete-function generation, not a representative stream of IDE edits. The initial 512 prompts were a feasibility choice under a small compute budget, not a statistical power calculation or a claim that Rust adaptation saturates there.</p>\n<h2 id=\"what-eagle-3-is-doing-here\">What EAGLE-3 is doing here</h2>\n<p>Speculative decoding lets a cheap draft propose future tokens and a more expensive target verify them. A useful draft predicts what the target would emit cheaply enough that accepting several tokens saves more work than drafting and verification add.</p>\n<p><a href=\"https://arxiv.org/abs/2503.01840\">EAGLE-3</a> is a form of speculative decoding. It uses target-model hidden features as input to its drafter. In this pinned Qwen implementation, the target exposes 4,096-dimensional vectors from zero-based layers 1, 17, and 32. Concatenating them produces 12,288 numbers per token; a learned projection maps that back to 4,096. The draft then uses a single transformer block. The target has 36 blocks.</p>\n<p>That same vector width does not make the draft as expensive or capable as the target: the draft performs far fewer transformations. Those activation vectors are temporary inputs, not compressed copies of Qwen's weights. This experiment uses BF16; it is <strong>not quantization</strong>. The exported draft stores 399,523,840 parameters excluding vocabulary buffers, uses the target embedding, and is not a standalone coding model.</p>\n<p>We warm-started <a href=\"https://huggingface.co/Tengyunw/qwen3_8b_eagle3\">Tengyunw/qwen3_8b_eagle3</a>, preserved its 32,000-entry draft vocabulary mapping, and trained only the draft. Qwen's responses supplied the training targets; its intermediate states supplied the features. The target still verifies draft proposals during serving. Training a better Rust drafter changes how efficiently we reach Qwen's answers, not Qwen's underlying ability to write Rust.</p>\n<p>The architecture, pretrained draft, serving engine, and training framework are upstream work. The contribution here is a controlled adaptation, the surrounding capture/export/benchmark pipeline, and an evidence-backed account of where it helps and where it does not.</p>\n<h2 id=\"data-and-training\">Data and training</h2>\n<p>The pinned Rust source is <a href=\"https://huggingface.co/datasets/Fortytwo-Network/Strandset-Rust-v1\">Fortytwo-Network/Strandset-Rust-v1</a>, a synthetic dataset. Crates were assigned to disjoint train, validation, and held-out splits. We kept 64 validation and 128 held-out prompts fixed while expanding the training pool. The larger pool retains the original 512 as an unchanged prefix. It contains 2,041 function-generation and seven completion prompts, spanning 890 training crates. Exact prompt deduplication does not establish semantic decontamination.</p>\n<p>We generated new answers with frozen Qwen3-8B, thinking disabled, and captured target features offline. The original pool has 108,939 teacher answer tokens; the full 2,048-example pool has 438,851. Five and 28 capped answers respectively were kept as prefixes without inventing endings. We did not train on the source's reference solutions. Python evaluation prompts come from <a href=\"https://huggingface.co/datasets/bigcode/humanevalpack\">HumanEvalPack</a>; no compiler or functional pass-rate evaluation was run.</p>\n<p>The mixed condition has 1,536 Rust and 512 general prompts. General instructions/context come from <a href=\"https://huggingface.co/datasets/databricks/databricks-dolly-15k\">Databricks Dolly 15k</a>, 64 examples from each of eight categories; answers were regenerated with Qwen. General examples are 25% of the mixture but 40.5% of its distinct teacher answer tokens. This is not a tool-call-specific training set. Dataset sources, fixed revisions, and redistribution notices accompany the release.</p>\n<p>All runs start from the same upstream draft, use BF16, one-example microbatches, eight-step accumulation, peak learning rate 1e-5, gradient clipping at 0.5, and EAGLE-3 training length seven. The cosine learning-rate horizon matches the 100- or 400-update budget. A separate Rust validation proxy, simulated acceptance length, selects the checkpoint; test timing does not. All listed candidates selected their final update. The 400-update runs resume full training state at update 200, including optimizer, scheduler, RNG, and data position.</p>\n<div class=\"rust-table-scroll\"><table>\n<thead>\n<tr>\n<th>Candidate</th>\n<th>Pool</th>\n<th style=\"text-align: right;\">Updates</th>\n<th style=\"text-align: right;\">Seed</th>\n<th style=\"text-align: right;\">Example exposures</th>\n<th style=\"text-align: right;\">Unique examples seen</th>\n<th style=\"text-align: right;\">Teacher answer-token exposures</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td><code>rust-draft</code> (pilot)</td>\n<td>512 Rust</td>\n<td style=\"text-align: right;\">100</td>\n<td style=\"text-align: right;\">20260918</td>\n<td style=\"text-align: right;\">800</td>\n<td style=\"text-align: right;\">512</td>\n<td style=\"text-align: right;\">167,427</td>\n</tr>\n<tr>\n<td><code>seed2</code></td>\n<td>512 Rust</td>\n<td style=\"text-align: right;\">100</td>\n<td style=\"text-align: right;\">20260919</td>\n<td style=\"text-align: right;\">800</td>\n<td style=\"text-align: right;\">512</td>\n<td style=\"text-align: right;\">167,736</td>\n</tr>\n<tr>\n<td><code>scale100</code></td>\n<td>2,048 Rust</td>\n<td style=\"text-align: right;\">100</td>\n<td style=\"text-align: right;\">20260918</td>\n<td style=\"text-align: right;\">800</td>\n<td style=\"text-align: right;\">800</td>\n<td style=\"text-align: right;\">173,209</td>\n</tr>\n<tr>\n<td><code>small400</code></td>\n<td>512 Rust</td>\n<td style=\"text-align: right;\">400</td>\n<td style=\"text-align: right;\">20260918</td>\n<td style=\"text-align: right;\">3,200</td>\n<td style=\"text-align: right;\">512</td>\n<td style=\"text-align: right;\">678,152</td>\n</tr>\n<tr>\n<td><code>scale400</code></td>\n<td>2,048 Rust</td>\n<td style=\"text-align: right;\">400</td>\n<td style=\"text-align: right;\">20260918</td>\n<td style=\"text-align: right;\">3,200</td>\n<td style=\"text-align: right;\">2,048</td>\n<td style=\"text-align: right;\">683,244</td>\n</tr>\n<tr>\n<td><code>mixed400</code></td>\n<td>1,536 Rust + 512 general</td>\n<td style=\"text-align: right;\">400</td>\n<td style=\"text-align: right;\">20260918</td>\n<td style=\"text-align: right;\">3,200</td>\n<td style=\"text-align: right;\">2,048</td>\n<td style=\"text-align: right;\">851,635</td>\n</tr>\n</tbody>\n</table></div>\n<p><em>Exposure counts reconstruct the training sampler and teacher-reported token usage. They are not exact loss-mask token counts or FLOPs. Equal updates do not guarantee equal compute.</em></p>\n<p><img loading=\"lazy\" alt=\"Validation progress\" src=\"/artifacts/rust-eagle3/phase2/figures/training-curves.png\" /></p>\n<p>The curves are validation proxies, not measured serving speed. Flattening as the learning rate approaches zero is not evidence of data saturation.</p>\n<h2 id=\"the-evaluation-contract\">The evaluation contract</h2>\n<p>The original pilot compared Qwen alone, Qwen with the upstream draft, and Qwen with the adapted draft. It used 32 held-out Rust prompts and 32 Python prompts, three repeats, and additional Rust concurrency checks. It found <strong>9.58%</strong> lower paired Rust latency versus the upstream draft at concurrency one (7.57–11.75%). Its 44.35% reduction versus Qwen alone largely reflects upstream EAGLE-3, and is not our incremental contribution.</p>\n<p>The follow-up froze 120 prompts before its GPU runs: 32 prior Rust, <strong>32 previously unused Rust</strong> as the primary endpoint, 32 Python, and eight each of prose, reasoning, and JSON-call diagnostics. The fresh Rust cohort spans 24 crates. Those eight-item diagnostics can expose trade-offs but cannot establish general capability retention.</p>\n<p>Each study evaluated all its candidates and the original draft sequentially on one H100, then reversed model order in a second session. The eight sessions landed on eight distinct H100 80GB GPUs. Each model received warmup and one measured pass per session. Startup, downloads, and training are excluded from request latency and included in cost accounting.</p>\n<p>Serving settings were fixed: Qwen revision, BF16, SGLang 0.5.18, temperature zero, thinking disabled, prefix cache disabled, 8,192-token context, 0.7 memory fraction, maximum 16 running requests, three speculative steps, top-k one, four draft slots, and a common 1,024-token output cap. The follow-up measures concurrency one only. Latency is end-to-end streaming HTTP request time; first-token timing uses the first nonempty content event, and token counts come from server usage rather than network chunks.</p>\n<p>For prompt <em>i</em>, first take its median baseline latency across the two sessions, Bᵢ, and median candidate latency, Cᵢ. Compute its reduction, 1 − Cᵢ/Bᵢ, then report the median across prompts. This is not the ratio of two pooled latency medians. We resample prompts 2,000 times with a fixed seed for an exploratory 95% interval. Session-specific results remain available. Crate-level bootstrap checks were added as a supplementary analysis after replication and are labeled accordingly.</p>\n<p>The frozen <a href=\"/artifacts/rust-eagle3/phase2/PROTOCOL.md\">protocol and chronological addenda</a> distinguish the original gate from later operational decisions. Intervals condition on the observed checkpoints, prompts, and sessions. They do not integrate uncertainty over new training corpora, all training seeds, or new deployments; they are not corrected for multiple comparisons.</p>\n<h2 id=\"result-1-the-initial-gain-replicated\">Result 1: the initial gain replicated</h2>\n<div class=\"rust-table-scroll\"><table>\n<thead>\n<tr>\n<th>Adaptation</th>\n<th style=\"text-align: right;\">Fresh-Rust reduction vs upstream draft</th>\n<th style=\"text-align: right;\">Exploratory 95% interval</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>512/100, original seed</td>\n<td style=\"text-align: right;\">10.66%</td>\n<td style=\"text-align: right;\">8.78–12.44%</td>\n</tr>\n<tr>\n<td>512/100, second seed</td>\n<td style=\"text-align: right;\">10.66%</td>\n<td style=\"text-align: right;\">8.61–12.69%</td>\n</tr>\n</tbody>\n</table></div>\n<p>Both seeds improved in both sessions with unchanged outputs on fresh Rust. This passed the predeclared gate for scaling. It weakens explanations confined to the first prompt sample, training seed, or serving order. The second seed still reuses the same teacher conversations; it is not a new corpus replication.</p>\n<p><img loading=\"lazy\" alt=\"Replication\" src=\"/artifacts/rust-eagle3/phase2/figures/replication.png\" /></p>\n<h2 id=\"result-2-longer-training-mattered-more-than-a-larger-pool\">Result 2: longer training mattered more than a larger pool</h2>\n<div class=\"rust-table-scroll\"><table>\n<thead>\n<tr>\n<th>Direct, same-session comparison on fresh Rust</th>\n<th style=\"text-align: right;\">Additional paired latency reduction</th>\n<th style=\"text-align: right;\">Exploratory 95% interval</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>512/100 → 2,048/100</td>\n<td style=\"text-align: right;\">0.09%</td>\n<td style=\"text-align: right;\">−0.03–1.21%</td>\n</tr>\n<tr>\n<td>2,048/100 → 2,048/400</td>\n<td style=\"text-align: right;\">5.08%</td>\n<td style=\"text-align: right;\">4.04–6.49%</td>\n</tr>\n<tr>\n<td>512/400 → 2,048/400</td>\n<td style=\"text-align: right;\">1.76%</td>\n<td style=\"text-align: right;\">0.40–2.84%</td>\n</tr>\n</tbody>\n</table></div>\n<p>At 100 updates, the larger-pool run saw only 800 of its available 2,048 examples. Increasing the pool without allowing time to use it produced little additional benefit. Comparing 100 with 400 updates changes optimization budget and the learning-rate horizon, not just exposure to new examples.</p>\n<p>The matched 400-update control addresses that confound. Both conditions use 3,200 example exposures and the same 400-update schedule. Their teacher answer-token exposures differ by only 0.75%, though this is not an exact FLOP match. The 512-example draft reduced fresh-Rust latency by <strong>14.47%</strong> versus the original; the 2,048-example draft reduced it by <strong>15.63%</strong> in the same study. The direct larger-versus-smaller benefit was 1.76%, positive in both sessions and in the supplementary crate bootstrap (0.49–2.53%).</p>\n<p>Native counters provide a separate mechanism check. On 16 fresh-Rust prompts repeated in both sessions, the 512/400 draft accepted 3,018 of 6,834 proposed tokens (<strong>44.16%</strong>); the 2,048/400 draft accepted 3,056 of 6,726 (<strong>45.44%</strong>). Verification steps fell from 2,278 to 2,242 with unchanged output. Bonus tokens are excluded from these acceptance ratios. These are separate probe requests, not the timed endpoint.</p>\n<p>The larger Rust draft is the strongest candidate tested for this workload. Two pool sizes and one seed per scaling condition do not identify a saturation curve. The original 512 examples were enough to demonstrate a gain, not enough to conclude that more data cannot help.</p>\n<h2 id=\"result-3-the-trade-offs-are-real\">Result 3: the trade-offs are real</h2>\n<p>Python was approximately unchanged. In the matched control, the eight JSON-call prompts slowed by about <strong>7.12%</strong> with the 512/400 draft and <strong>5.99%</strong> with the 2,048/400 draft versus the upstream draft. Median paired increases were about 8.5 ms and 7.0 ms respectively; the original draft's pooled median on this short workload was about 113.6 ms. These are small absolute differences but they contradict a blanket no-regression claim.</p>\n<p>The mixed draft retained a <strong>15.05%</strong> Rust latency reduction versus the original (13.67–15.63%) in its own sessions. Directly against the Rust-only 2,048/400 draft:</p>\n<div class=\"rust-table-scroll\"><table>\n<thead>\n<tr>\n<th>Workload</th>\n<th style=\"text-align: right;\">Mixed-draft latency effect</th>\n<th style=\"text-align: right;\">Exploratory 95% interval</th>\n</tr>\n</thead>\n<tbody>\n<tr>\n<td>Fresh Rust</td>\n<td style=\"text-align: right;\">1.44% slower</td>\n<td style=\"text-align: right;\">0.20–1.74% slower</td>\n</tr>\n<tr>\n<td>Prose</td>\n<td style=\"text-align: right;\">4.02% faster</td>\n<td style=\"text-align: right;\">1.43–6.98% faster</td>\n</tr>\n<tr>\n<td>JSON calls</td>\n<td style=\"text-align: right;\">0.04% faster</td>\n<td style=\"text-align: right;\">−0.23–5.60% reduction</td>\n</tr>\n</tbody>\n</table></div>\n<p>Python and reasoning differences were inconclusive. Both sessions agreed on the Rust/prose directions. The direct JSON interval includes zero, so we cannot claim the mix repaired that slowdown. Subtracting the baseline-relative medians would produce a misleading impression of a larger benefit: paired medians are not transitive.</p>\n<p><img loading=\"lazy\" alt=\"Mixed-data trade-off\" src=\"/artifacts/rust-eagle3/phase2/figures/mixed.png\" /></p>\n<p>The mixed run saw 24.65% more teacher answer-token exposures than the Rust-only run. Its prose gain therefore cannot be attributed purely to mixture composition at matched compute. All candidates were selected on Rust validation. This is one fixed mixture, not an optimized universal drafter.</p>\n<h2 id=\"integrity-checks-and-failure-modes\">Integrity checks and failure modes</h2>\n<p>All <strong>3,120 follow-up timed requests succeeded</strong>. All <strong>120 unique prompts had identical text and completion-token counts across the 26 saved observations per prompt</strong>. The 26 capped requests are repeats of the same prior-Rust prompt; no fresh-Rust request reached the cap. The 480 separate native-probe requests also preserved text within each comparison. The release includes raw outputs, prompt hashes, run settings, and a CPU-only verifier that recomputes the paired comparisons.</p>\n<p>This finite agreement is useful engineering evidence, not a proof of stochastic-distribution equivalence or Rust correctness. Follow-up comparisons use the original draft as the reference; they do not include a fresh Qwen-only arm for every new prompt. A saved cross-check against pilot Qwen-only output covers the 64 overlapping Rust/Python prompts.</p>\n<p>The pilot's concurrency-four and concurrency-sixteen outputs varied, even in target-only repeats, including actual code differences. Numerical and scheduling effects are plausible, but were not isolated. We retain those measurements and do not present their timing as an unqualified same-output speedup. Production concurrency remains unvalidated.</p>\n<p>Other limits are a single target/draft pair, one GPU family, synthetic prompts, short diagnostic cohorts, no independently sampled training corpus, and no matched all-general-code distillation control. The latter means we cannot isolate language specialization from the benefit of continued distillation itself. No compilation, functional code pass rate, quantization, or stochastic sampling study ran.</p>\n<h2 id=\"engineering-details-worth-preserving\">Engineering details worth preserving</h2>\n<p>The pipeline covers target-response generation, offline feature capture, warm-start training, resumed training, audited export, streaming benchmarks, native acceptance counters, and paired analysis. A small <a href=\"/artifacts/rust-eagle3/patches/specforge-preformatted-capture.patch\">SpecForge patch</a> repairs loading of preformatted capture inputs. Export preserves the pretrained vocabulary mapping rather than silently assigning new meanings to output-head rows. Tokenizer return-type handling and server-process cleanup were checked locally.</p>\n<p>Interrupted pilot attempts and a later packaging failure were excluded from completed performance comparisons and included in conservative cost tracking. The packaging failure occurred before GPU execution when a generated CSV changed during upload; restricting uploads to code and frozen inputs resolved it. These fixes did not involve tuning model or decoding settings from final test results.</p>\n<p>The cached GPU image was reused after its initial dependency resolution. Exact model, data, SpecForge, and observed package revisions are recorded, but a clean reconstruction of that image has not been independently tested. The public code has additional namespace/budget safeguards; those publication-only changes were checked on CPU, not with another paid training run.</p>\n<h2 id=\"cost-and-artifacts\">Cost and artifacts</h2>\n<p>Provider-reported completed usage was <strong>$17.27 before credits</strong>, including the final app; the independent conservative launch ledger closed at <strong>$41.88</strong>, under the cumulative <strong>$60</strong> cap. These are different accounting measures, not two charges. Provider usage is not a final invoice, and retained storage is separate. All experiment GPU apps were stopped and no active containers remained at closeout. Preparing this release used no new GPU jobs.</p>\n<p>The package contains the experiment source, fixed inputs, generated training conversations, per-request measurements, comparisons, training audits, figures, checkpoint cards, checksums, and upstream notices. Six model bundles are prepared separately from Git history. The principal Rust candidate is <code>scale400</code>; <code>mixed400</code> is a documented Rust/prose trade-off, not an unconditional replacement.</p>\n<p>Start with <a href=\"/artifacts/rust-eagle3/REPRODUCE.md\">reproduction instructions</a>, the <a href=\"/artifacts/rust-eagle3/ARTIFACTS.md\">artifact index</a>, and <a href=\"/artifacts/rust-eagle3/NOTICE.md\">source and license notices</a>. The <a href=\"/artifacts/rust-eagle3/PUBLICATION_CHANGES.md\">publication changes</a> explain sanitization and how the public copy differs from the private execution record.</p>\n<h2 id=\"references\">References</h2>\n<ul>\n<li>EAGLE-3 authors, <a href=\"https://arxiv.org/abs/2503.01840\">EAGLE-3: Scaling up Inference Acceleration of Large Language Models via Training-Time Test</a>.</li>\n<li>Qwen team, <a href=\"https://huggingface.co/Qwen/Qwen3-8B\">Qwen3-8B</a>, target revision in <a href=\"/artifacts/rust-eagle3/configs/sources.json\">sources</a>.</li>\n<li>Tengyunw, <a href=\"https://huggingface.co/Tengyunw/qwen3_8b_eagle3\">Qwen3-8B EAGLE-3 draft</a>, warm-start checkpoint.</li>\n<li>SGLang team, <a href=\"https://github.com/sgl-project/sglang\">SGLang</a> and <a href=\"https://github.com/sgl-project/SpecForge\">SpecForge</a>, serving and training frameworks.</li>\n<li>Fortytwo Network, <a href=\"https://huggingface.co/datasets/Fortytwo-Network/Strandset-Rust-v1\">Strandset-Rust-v1</a>; BigCode, <a href=\"https://huggingface.co/datasets/bigcode/humanevalpack\">HumanEvalPack</a>; Databricks, <a href=\"https://huggingface.co/datasets/databricks/databricks-dolly-15k\">Dolly 15k</a>.</li>\n</ul>";
export default body;
